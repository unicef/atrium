{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "test": "lerna run test",
    "dev": "lerna run start --parallel",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "start": "docker-compose -f docker-compose-dev.yml up --build",
    "start:nobuild": "docker-compose -f docker-compose-dev.yml up"
  }
}
